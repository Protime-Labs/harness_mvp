"""Domain layer (B0 contracts, B1 invariants, B5 gate, risk & aggregation).

PURE: standard library only, no I/O, no model calls, no framework imports.
Everything here is deterministic and unit-testable in isolation.
"""
