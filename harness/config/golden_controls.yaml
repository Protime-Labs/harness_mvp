# B6 — Golden Controls mapping (BF-17). Owner: enterprise governance.
# This is a STRUCTURED UNRESOLVED DEPENDENCY RECORD, not a set of real control IDs. Until the enterprise
# Golden Controls catalogue is supplied, findings map to the fallback OWASP/NIST tags below and
# enterprise control attestation is explicitly NOT complete (readiness + reports say so).
schema: harness/golden-controls/v1
status: unresolved
external_dependency: enterprise Golden Controls catalogue
note: >
  No real control IDs are asserted here. Replace each domain with its true control_id + text when
  the catalogue is provided; until then the fallback standards carry the mapping.
domains:
  tool_egress:
    status: unresolved
    fallback_standards: { owasp_llm: [LLM06], nist_ai_rmf: [MANAGE] }
    blocks_enterprise_attestation: true
  change_mgmt:
    status: unresolved
    fallback_standards: { owasp_llm: [LLM08], nist_ai_rmf: [GOVERN] }
    blocks_enterprise_attestation: true
  model_risk:
    status: unresolved
    fallback_standards: { nist_ai_rmf: [MEASURE], iso_42001: [clause-8] }
    blocks_enterprise_attestation: true
  data_policy:
    status: unresolved
    fallback_standards: { owasp_llm: [LLM02], nist_ai_rmf: [MAP] }
    blocks_enterprise_attestation: true
  acceptable_use:
    status: unresolved
    fallback_standards: { owasp_llm: [LLM01], nist_ai_rmf: [MANAGE] }
    blocks_enterprise_attestation: true
  cpni_handling:
    status: unresolved
    fallback_standards: { owasp_llm: [LLM02], nist_ai_rmf: [MEASURE] }
    blocks_enterprise_attestation: true
